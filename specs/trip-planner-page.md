Things that need to change:
Trip Planner Heading in the top left should be "TrailForge" instead. TrailForge should be clickable and clicking it should take you back to the dashboard. I think there should also be a small back button as well, perhaps? On the left nav bar, at the top should be the "Trip Planner" heading. The place waypoint and draw route buttons on the map are placed over the zoom in and out buttons on the map. These should probably be moved. Maybe bottom right corner? The "Map", "Gear", "Itinerary", and "Conditions" buttons are cut off because the left nav is smaller than the horizontal text. Similarly, on the gear page the all items, checklist, by category, and by... (something else that gets cut off) has the same cut off issue as the different pages as just described.