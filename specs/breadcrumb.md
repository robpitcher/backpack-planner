- I still want "TrailForge" to be a clickable link on the dashboard homepage.

- When selected on a trip from the dashboard, remove the back arrow in the top left and make sure the TrailForge heading is clickable and clicking it takes you back to the overall dashboard view.

- When you click into a trip and go into the trip planner view, make it so the breadcrumb menu shows TrailForge -> Trip Planner instead of the trip name because the trip name is already in the left sidebar nav.

- Remove the waypoint stuff from the breadcrumb menu (waypoints and the waypoint name). I just want it to go as deep as "Trip Planner" for now.

- Clicking on Trip Planner in the breadcrumb menu should revert back to the initial trip planner view (as if you had just clicked into it from the dashboard), meaning that the map view should show all the trips waypoints.

- When selected on a waypoint, if a user clicks in a blank area in the sidebar nav, i want the waypoint to be deselected. When deselected, the map should revert to the overall trip planner view that shows all waypoints.